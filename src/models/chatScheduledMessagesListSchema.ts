/**
 * Slack Web APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  ResponseMetadata,
  responseMetadataSchema,
} from './responseMetadata.js';
import {
  ScheduledMessage,
  scheduledMessageSchema,
} from './scheduledMessage.js';

/** Schema for successful response from chat.scheduledMessages.list method */
export interface ChatScheduledMessagesListSchema {
  ok: string;
  responseMetadata: ResponseMetadata;
  scheduledMessages: ScheduledMessage[];
  additionalProperties?: Record<string, unknown>;
}

export const chatScheduledMessagesListSchemaSchema: Schema<ChatScheduledMessagesListSchema> = lazy(
  () =>
    typedExpandoObject(
      {
        ok: ['ok', string()],
        responseMetadata: ['response_metadata', responseMetadataSchema],
        scheduledMessages: [
          'scheduled_messages',
          array(scheduledMessageSchema),
        ],
      },
      'additionalProperties',
      optional(unknown())
    )
);
