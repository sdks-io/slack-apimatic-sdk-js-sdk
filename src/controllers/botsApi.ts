/**
 * Slack Web APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  BotsInfoSchema,
  botsInfoSchemaSchema,
} from '../models/botsInfoSchema.js';
import { optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { BotsInfoErrorSchemaError } from '../errors/botsInfoErrorSchemaError.js';

export class BotsApi extends BaseApi {
  /**
   * Gets information about a bot user.
   *
   * @param token        Authentication token. Requires scope: `users:read`
   * @param bot          Bot user to get info on
   * @return Response from the API call
   */
  async botsInfo(
    token: string,
    bot?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BotsInfoSchema>> {
    const req = this.createRequest('GET', '/bots.info');
    const mapped = req.prepareArgs({
      token: [token, string()],
      bot: [bot, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.query('token', mapped.token);
    req.query('bot', mapped.bot);
    req.defaultToError(
      BotsInfoErrorSchemaError,
      'When no bot can be found, it returns an error.'
    );
    req.authenticate([{ slackAuth: true }]);
    return req.callAsJson(botsInfoSchemaSchema, requestOptions);
  }
}
