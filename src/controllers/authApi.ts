/**
 * Slack Web APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  AuthRevokeSchema,
  authRevokeSchemaSchema,
} from '../models/authRevokeSchema.js';
import {
  AuthTestSuccessSchema,
  authTestSuccessSchemaSchema,
} from '../models/authTestSuccessSchema.js';
import { boolean, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { AuthRevokeErrorSchemaError } from '../errors/authRevokeErrorSchemaError.js';
import { AuthTestErrorSchemaError } from '../errors/authTestErrorSchemaError.js';

export class AuthApi extends BaseApi {
  /**
   * Revokes a token.
   *
   * @param token        Authentication token. Requires scope: `none`
   * @param test         Setting this parameter to `1` triggers a _testing mode_ where the specified token
   *                                will not actually be revoked.
   * @return Response from the API call
   */
  async authRevoke(
    token: string,
    test?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AuthRevokeSchema>> {
    const req = this.createRequest('GET', '/auth.revoke');
    const mapped = req.prepareArgs({
      token: [token, string()],
      test: [test, optional(boolean())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.query('token', mapped.token);
    req.query('test', mapped.test);
    req.defaultToError(AuthRevokeErrorSchemaError, 'Typical error response');
    req.authenticate([{ slackAuth: true }]);
    return req.callAsJson(authRevokeSchemaSchema, requestOptions);
  }

  /**
   * Checks authentication & identity.
   *
   * @param token        Authentication token. Requires scope: `none`
   * @return Response from the API call
   */
  async authTest(
    token: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AuthTestSuccessSchema>> {
    const req = this.createRequest('GET', '/auth.test');
    const mapped = req.prepareArgs({ token: [token, string()] });
    req.header('token', mapped.token);
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.defaultToError(
      AuthTestErrorSchemaError,
      'Standard failure response when used with an invalid token'
    );
    req.authenticate([{ slackAuth: true }]);
    return req.callAsJson(authTestSuccessSchemaSchema, requestOptions);
  }
}
