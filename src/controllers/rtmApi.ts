/**
 * Slack Web APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  RtmConnectSchema,
  rtmConnectSchemaSchema,
} from '../models/rtmConnectSchema.js';
import { boolean, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { RtmConnectErrorSchemaError } from '../errors/rtmConnectErrorSchemaError.js';

export class RtmApi extends BaseApi {
  /**
   * Starts a Real Time Messaging session.
   *
   * @param token                Authentication token. Requires scope: `rtm:stream`
   * @param batchPresenceAware   Batch presence deliveries via subscription. Enabling changes the shape of
   *                                        `presence_change` events.
   * @param presenceSub          Only deliver presence events when requested by subscription
   * @return Response from the API call
   */
  async rtmConnect(
    token: string,
    batchPresenceAware?: boolean,
    presenceSub?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RtmConnectSchema>> {
    const req = this.createRequest('GET', '/rtm.connect');
    const mapped = req.prepareArgs({
      token: [token, string()],
      batchPresenceAware: [batchPresenceAware, optional(boolean())],
      presenceSub: [presenceSub, optional(boolean())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.query('token', mapped.token);
    req.query('batch_presence_aware', mapped.batchPresenceAware);
    req.query('presence_sub', mapped.presenceSub);
    req.defaultToError(RtmConnectErrorSchemaError, 'Typical error response');
    req.authenticate([{ slackAuth: true }]);
    return req.callAsJson(rtmConnectSchemaSchema, requestOptions);
  }
}
