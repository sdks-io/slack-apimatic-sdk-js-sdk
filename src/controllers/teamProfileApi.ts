/**
 * Slack Web APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  TeamProfileGetSuccessSchema,
  teamProfileGetSuccessSchemaSchema,
} from '../models/teamProfileGetSuccessSchema.js';
import { optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { TeamProfileGetErrorSchemaError } from '../errors/teamProfileGetErrorSchemaError.js';

export class TeamProfileApi extends BaseApi {
  /**
   * Retrieve a team's profile.
   *
   * @param token        Authentication token. Requires scope: `users.profile:read`
   * @param visibility   Filter by visibility.
   * @return Response from the API call
   */
  async teamProfileGet(
    token: string,
    visibility?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TeamProfileGetSuccessSchema>> {
    const req = this.createRequest('GET', '/team.profile.get');
    const mapped = req.prepareArgs({
      token: [token, string()],
      visibility: [visibility, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.query('token', mapped.token);
    req.query('visibility', mapped.visibility);
    req.defaultToError(
      TeamProfileGetErrorSchemaError,
      'Typical error response'
    );
    req.authenticate([{ slackAuth: true }]);
    return req.callAsJson(teamProfileGetSuccessSchemaSchema, requestOptions);
  }
}
