/**
 * Slack Web APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  AppsUninstallSchema,
  appsUninstallSchemaSchema,
} from '../models/appsUninstallSchema.js';
import { optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { AppsUninstallErrorSchemaError } from '../errors/appsUninstallErrorSchemaError.js';

export class AppsApi extends BaseApi {
  /**
   * Uninstalls your app from a workspace.
   *
   * @param token         Authentication token. Requires scope: `none`
   * @param clientId      Issued when you created your application.
   * @param clientSecret  Issued when you created your application.
   * @return Response from the API call
   */
  async appsUninstall(
    token?: string,
    clientId?: string,
    clientSecret?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AppsUninstallSchema>> {
    const req = this.createRequest('GET', '/apps.uninstall');
    const mapped = req.prepareArgs({
      token: [token, optional(string())],
      clientId: [clientId, optional(string())],
      clientSecret: [clientSecret, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.query('token', mapped.token);
    req.query('client_id', mapped.clientId);
    req.query('client_secret', mapped.clientSecret);
    req.defaultToError(AppsUninstallErrorSchemaError, 'Typical error response');
    req.authenticate([{ slackAuth: true }]);
    return req.callAsJson(appsUninstallSchemaSchema, requestOptions);
  }
}
