/**
 * Slack Web APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  ChatScheduledMessagesListSchema,
  chatScheduledMessagesListSchemaSchema,
} from '../models/chatScheduledMessagesListSchema.js';
import { number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { ChatScheduledMessagesListErrorSchemaError } from '../errors/chatScheduledMessagesListErrorSchemaError.js';

export class ChatScheduledMessagesApi extends BaseApi {
  /**
   * Returns a list of scheduled messages.
   *
   * @param token        Authentication token. Requires scope: `none`
   * @param channel      The channel of the scheduled messages
   * @param latest       A UNIX timestamp of the latest value in the time range
   * @param oldest       A UNIX timestamp of the oldest value in the time range
   * @param limit        Maximum number of original entries to return.
   * @param cursor       For pagination purposes, this is the `cursor` value returned from a previous call
   *                               to `chat.scheduledmessages.list` indicating where you want to start this call from.
   * @return Response from the API call
   */
  async chatScheduledMessagesList(
    token?: string,
    channel?: string,
    latest?: number,
    oldest?: number,
    limit?: number,
    cursor?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ChatScheduledMessagesListSchema>> {
    const req = this.createRequest('GET', '/chat.scheduledMessages.list');
    const mapped = req.prepareArgs({
      token: [token, optional(string())],
      channel: [channel, optional(string())],
      latest: [latest, optional(number())],
      oldest: [oldest, optional(number())],
      limit: [limit, optional(number())],
      cursor: [cursor, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.header('token', mapped.token);
    req.query('channel', mapped.channel);
    req.query('latest', mapped.latest);
    req.query('oldest', mapped.oldest);
    req.query('limit', mapped.limit);
    req.query('cursor', mapped.cursor);
    req.defaultToError(
      ChatScheduledMessagesListErrorSchemaError,
      'Typical error response if the channel passed is invalid'
    );
    req.authenticate([{ slackAuth: true }]);
    return req.callAsJson(
      chatScheduledMessagesListSchemaSchema,
      requestOptions
    );
  }
}
